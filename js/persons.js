// Rubel's vote count //
function countVoteRubel (){
    const vote = getCurrentvoteValueById('rubel-vote');
    setValueById('rubel-vote', vote)
}

// Sorna's vote count //
function countVoteSorna(){
    const vote = getCurrentvoteValueById('robbu-vote');
    setValueById('robbu-vote', vote)
}

// Sumaiya's vote count //
function countVoteSumaiya (){
    const vote = getCurrentvoteValueById('milon-vote');
    setValueById('milon-vote', vote)
}

//Sapon's vote count //
function countVoteSapon(){
    const vote = getCurrentvoteValueById('shariya-vote');
    setValueById('shariya-vote', vote);
}